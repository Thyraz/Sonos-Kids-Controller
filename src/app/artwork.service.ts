import { Injectable } from '@angular/core';

@Injectable({
  providedIn: 'root'
})
export class ArtworkService {

  constructor() { }

  // https://itunes.apple.com/search?term=was+ist+was+junior+polizei&entity=album
  // Cache locally as image with name of search term?
}
